import 'package:flutter/foundation.dart';

/// Вопрос, с которым человек уходит в Alma с другого экрана.
///
/// **Зачем признак, а не вызов.** Спрашивающий (читалка гороскопа, `R2`) живёт
/// в маршруте поверх всей оболочки и о вкладках не знает вовсе; отвечающий
/// (`AlmaScreen`) живёт в четвёртой странице `PageView` и о читалке не знает
/// тем более. Протащить между ними обратный вызов значило бы связать два
/// экрана, которые никогда не видят друг друга на кадре. Признак один на
/// приложение — ровно как [readingNow] для полосы вкладок.
///
/// **Порядок читателей важен и он не случаен.** Слушателя двое: оболочка
/// (`main.dart`) переключает вкладку, экран Alma подставляет текст в поле и
/// **гасит признак**. Оболочка подписывается в `initState`, то есть до того,
/// как построятся её дети, — значит она получает событие первой, и вкладка уже
/// сменилась к моменту, когда поле заполняется.
///
/// **Заполняет, но не отправляет.** Вопросов в день три, и тот, кто ткнул в
/// карточку посмотреть, что будет, потратил бы на этом треть нормы. То же
/// правило, что у готовых вопросов на самом экране Alma.
final ValueNotifier<String?> almaDraft = ValueNotifier(null);
