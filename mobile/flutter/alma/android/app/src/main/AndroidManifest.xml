<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- **Разрешение на уведомления, и оно спрашивается вторым.**

         С Android 13 (API 33) показать уведомление без него нельзя, и оно
         обычное runtime-разрешение: объявления в консоли Play не требует, но
         спрашивается у человека и после отказа **больше не спрашивается
         системой** до переустановки приложения. Отсюда порядок, записанный в
         docs/PUSH.md §5.5: сначала наш экран пред-вопроса, который можно
         показать снова, и только по его «Разрешить» — системное окно, которого
         второй раз не будет.

         Приложение целится в 36 (flutter.targetSdkVersion), а значит момент
         вопроса выбирает само. Целься оно в 32 и ниже — систему спросила бы за
         нас, при первом же запуске, без всякого объяснения. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- **Автобэкап выключён, и это не осторожность, а условие работы.**
         `flutter_secure_storage` шифрует ключом из Keystore, а Keystore не
         уезжает в бэкап; восстановленная из Google Drive копия отдаёт
         зашифрованный токен и ключ, которым его не расшифровать —
         InvalidKeyException при первом же запросе. Флаг обязан стоять до того,
         как токен туда поедет. -->
    <application
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:label="alma"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
